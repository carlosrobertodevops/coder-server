# === URL pública do seu Coder (use seu domínio com HTTPS) ===
CODER_ACCESS_URL=https://coder.mondaha.com

# === Senha do Postgres usado pelo Coder ===
POSTGRES_PASSWORD=troque-por-uma-senha-forte

# === GID do grupo 'docker' no host (permite acesso ao /var/run/docker.sock)
# Pegue na VPS com: getent group docker | cut -d: -f3
DOCKER_GID=999

# === Telemetria do Coder (opcional: false para desativar)
CODER_TELEMETRY=false

# --- OPCIONAIS (já estão fixos no compose, mas pode sobrescrever se quiser) ---
# POSTGRES_DB=coder
# POSTGRES_USER=coder
